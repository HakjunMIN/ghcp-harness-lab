[project]
name = "sample-backend"
version = "0.0.0"
description = "Minimal backend project for tests"
requires-python = ">=3.11"

dependencies = [
  "fastapi>=0.95",
  "pydantic>=1.10",
  "pytest>=7.0",
  "httpx>=0.24",
  "uvicorn>=0.22",
  "azure-identity>=1.13",
  "requests>=2.31",
]

[tool.uv]
# use default settings; listing for clarity

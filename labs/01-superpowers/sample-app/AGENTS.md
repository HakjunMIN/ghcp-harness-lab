# Agent context for `mdtodo` sample app

You are working inside lab 01 (Superpowers). The lab README is at
`../README.md`. The brief is in `BRIEF.md`. Always:

- Read BRIEF.md first.
- Use Superpowers skills (brainstorming → writing-plans →
  subagent-driven-development → test-driven-development →
  requesting-code-review → finishing-a-development-branch) in order.
- Only Python standard library. No new dependencies.
- Save design output to DESIGN.md, plan output to PLAN.md, and the final
  retrospective to RETRO.md.
- Run `python3 -m unittest discover -s tests` for verification.
